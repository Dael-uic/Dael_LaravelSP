<div class="d-flex justify-content-center gap-2">
    <a href="{{ route('students.show', $id) }}" class="btn btn-primary btn-sm"><i class="fas fa-eye"></i> View</a>
    <a href="{{ route('students.edit', $id) }}" class="btn btn-warning btn-sm"><i class="fas fa-edit"></i> Edit</a>
</div>